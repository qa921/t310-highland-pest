---
import "../styles/global.css";
import { site } from "../data/site";

interface Props {
  title: string;
  description?: string;
}

const { title, description = site.description } = Astro.props;

const nav = [
  { href: "/areas/denver", label: "Denver", primary: true },
  { href: "/services", label: "Services" },
  { href: "/areas", label: "Service areas" },
  { href: "/articles", label: "Articles" },
  { href: "/about", label: "About" },
  { href: "/contact", label: "Contact" },
];
---
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="description" content={description} />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <title>{title} | {site.brand}</title>
  </head>
  <body>
    <header class="site-header">
      <a class="brand" href="/">{site.brand}</a>
      <p class="market-tag">Centred on {site.primaryMarket}, our primary market — plus confirmed supporting areas</p>
      <nav aria-label="Primary">
        <ul>
          {nav.map((item) => (
            <li class={item.primary ? "nav-primary" : undefined}>
              <a href={item.href}>{item.label}{item.primary ? " (primary market)" : ""}</a>
            </li>
          ))}
        </ul>
      </nav>
    </header>
    <main>
      <slot />
    </main>
    <footer class="site-footer">
      <p>{site.brand} — enquiry-led pest and wildlife exclusion information centred on {site.primaryMarket}, with confirmed supporting areas listed on our <a href="/areas">service areas</a> page. No prices, availability, or guarantee claims are published on this site.</p>
      <ul class="footer-links">
        <li><a href="/contact">General enquiry</a></li>
        <li><a href="/privacy">Privacy</a></li>
        <li><a href="/sitemap.xml">Sitemap</a></li>
      </ul>
    </footer>
  </body>
</html>
